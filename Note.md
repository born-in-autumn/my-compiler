此文档记录我思考的过程，会很杂乱

我们得自己制定一套规则才行，也就是说从现在开始要考虑寄存器的分配问题了对吧
我今天思考了一下，我自己有一些思路
1.  x1是一个留给printf打印的寄存器，任何有要打印的值，我都在之前扔到x1上再打印，为了清晰，我决定让其他的值都不放在x1寄存器上，而是从x2开始。并且现在规定栈永远是真正的家，而寄存器只负责算，寄存器的东西可以随意覆盖，所以寄存器上的任何东西，存上去就立马消费，以防后续被覆盖
2. 之前写的那些，如何计数，一开始算出sp偏移的位置，这些都是没问题的，可以继续保留，怎样同时存a和b也很简单，和现在的思路差不多，看见a，先拿到a的值，然后把值扔到x2上，然后立马把x2的值复制到栈里的某个位置，因为我们有hashmap并且我们始终在记录存了几个值（已有逻辑），所以这是能做到的，然后这个值作为value，a作为key存到hashmap里。然后是b，也是如此，不赘述，b应该会存到a的下一个16字节的位置上。接着就是c，重头戏来了，c=b+a，这不是一个立即数，我在想一件事情，codegen的时候是不是就可以把c的值算出来了，这样c又变成了一个立即数？但是这样到底好不好呢？不知道你怎么看？
3. 我就假设c没有算出来，我们可以做的是：立马把b和c扔到寄存器上，x2，x3，为啥是x2、x3呢，因为前面说过了，这些寄存器只用来当场使用，从来不存任何东西，所以上面的值可以随意覆盖，然后，x2+x3的值扔给x2，最后把x2扔到栈上的下一个16字节，这个位置就是c的值
4. 未来要读谁，就把谁扔到x1，扔上去就直接读
